@pkg = github.com/burstsms/mtmo-tp/backend
@run = go run -mod=vendor

api/**/*.go {
  daemon: @run ./api/server 
}

# account service and workers
service/account/**/*.go lib/**/*.go {
  daemon: @run ./account/rpc/server 
}

